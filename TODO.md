# 追加したい機能メモ

思いついた機能アイデアをここに書き留めておきます。実装の要否・詳細は都度相談して決めます。

## 1. 日程未定の行事の一時保存(候補プール)機能

**きっかけ・目的**
入れたい行事などの日程がまだ決まっていない場合に、日付に紐付けず「一次保存」的に置いておき、日程が決まり次第、年間予定入力の該当日に組み込めるようにしたい。翌年度の予定を検討する段階(まだ年度が始まっていない/日付未定の行事案を溜めておきたい時)にも便利。

**想定する使い勝手**
- 「学校行事等(年間行事)」などの入力欄をクリックすると、直接入力ではなく専用のウィンドウ(モーダル)が開く
- そのウィンドウ内で、①自由入力 ②一次保存リストから選択、のどちらかができる
- 一次保存リストから選ぶと、その日のマスに反映される

**検討が必要な点(未整理・要相談)**
- 一次保存プールのデータ構造(state に新しい配列/リストを追加するイメージ)
- どの入力欄に適用するか。「年間行事」「月のみ」「教員のみ」で共通のプールにするか、欄ごとに分けるか
- 一次保存から組み込んだ後、プール側の項目を消すか、残して複数の日に使い回せるようにするか
- 現状は欄をクリックすると直接カーソルが入って自由入力できる方式。これを全部モーダル化すると、かえって毎回のクリック数が増えて手間にならないか(モーダルを開く欄・開かない欄の切り分けが必要かもしれない)

## 2. 教科別の授業実績入力と、それに基づく時数予測機能

**きっかけ・目的**
現状、①〜⑥欄は「時限の便宜番号」を入力するだけで、どの教科(国語・数学など)かまでは記録していない(HANDOFF.md の「既知の未実装・今後の課題」にも記載)。教科ごとの授業実績を入れられるようにし、そこから「このペースで進むと年度末までに教科ごとの時数がどうなるか」を予測できるようにしたい。

**想定する使い勝手(ラフ案・要相談)**
- 既存の「年間予定入力」タブの①〜⑥欄(便宜番号)はそのまま残し、それとは別に、教科別の授業「実績」を入れる場所を新設する(新しいタブ、または「時数・集計」タブの中に追加するイメージ)
- 実績入力済みの教科別コマ数と、今後の予定日(まだ実績が入っていない残りの授業日)を組み合わせて、年度末時点の教科別時数を予測
- 既存の「曜日・時限別の実施回数」集計(時数・集計タブ)や「必要時数との比較」機能と組み合わせられそう

**検討が必要な点(未整理・要相談)**
- 時間割(曜日×時限→教科)をどう持つか。学年ごと・学期途中の時間割変更にどう対応するか
- 「予測」の計算方法(残りの授業日数 × 通常の時間割 で単純に見込むか、行事等によるカットも加味するか)
- 新設する実績入力欄と、既存の①〜⑥入力(便宜番号のみ)との関係(重複入力にならないか、どちらが正になるか)

## 3. Excel出力の書式を、実際に使っている行事予定ファイルの見た目に合わせて調整 — 対応済み(2026-09-07)

**きっかけ・目的**
現在アプリから出力されるExcelファイルは、セルの色が付いていない部分や、罫線が引かれていない部分がある。実際に学校で使っている(元になった)行事予定のExcelファイルの各シートを参考に、色付け・罫線などの書式を合わせたい。

**調査結果(原本ファイルを提供いただき分析)**
- 原本(4月職/4月生/年(職員)/年(生徒))は、見出し・データグリッドの全セルに隙間なく細罫線が入っており、結合セルも「隠れる側」のセルまで含めて罫線が設定されている。逆に、静的な色分け(土曜=青、日曜=赤など)は無く、色付けは「休/長休/欠」といった出席状況を条件付き書式で灰色表示する仕組みのみだった。
- アプリ側の罫線抜け・色抜けの原因は、見出し行や結合セルの「隠れる側」のマス、月間予定表の週番結合セルの陰の日、年間予定表で日数が31日に満たない月の余り部分などを、データを何も入れない(=罫線設定も無い)まま出力していたこと。

**対応内容**
- 月間予定表(教員用/家庭用)の見出し2行(row1/row2)で、結合セルの陰になる位置も含めて全マスに罫線付きの空セルを設定
- 週番のセル結合で「隠れる側」の日も罫線付きの空セルに変更(以前はnullで罫線が消えていた)
- 年間予定表(教員用/家庭用)の月見出し・年計/学期計/月計の結合セルの陰、および31日に満たない月の余りマスに、罫線付きの空セルを設定
- 土日祝の色分け(薄いグレー)は従来通り維持(原本の条件付き書式とは仕組みが異なるが、視認性のため残す方針)

**保留(今回は対応せず)**
- 原本にある「6列/3列ごとの太い罫線での区切り」(週・月のグループ境界)は、細罫線への統一のみで今回は見送り。必要なら別途対応可能。
