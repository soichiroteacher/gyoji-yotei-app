# 追加したい機能メモ

思いついた機能アイデアをここに書き留めておきます。実装の要否・詳細は都度相談して決めます。

## 1. 日程未定の行事の一時保存(候補プール)機能 — 対応済み(既存機能で充足)

**きっかけ・目的**
入れたい行事などの日程がまだ決まっていない場合に、日付に紐付けず「一次保存」的に置いておき、日程が決まり次第、年間予定入力の該当日に組み込めるようにしたい。翌年度の予定を検討する段階(まだ年度が始まっていない/日付未定の行事案を溜めておきたい時)にも便利。

**確認の結果**
「学校行事等(年間・月間・教員のみ)」欄をクリックすると開く入力モーダル(`openEventPickerModal`)に、既にこの機能一式が実装済みだった。①自由入力の枠、②`state.eventCandidates`を使った候補プールからの選択、の両方に対応しており、4つの欄(年間/月間/教員のみ年間/教員のみ月間)で共通のプールを使用、候補は削除するまで残り複数の日で使い回せる、検索・使用済み非表示・くり返し使う候補の例外指定にも対応済み(2026-09-09までに実装)。新たな実装は不要と判断。

**追記・使用済みを既定で隠すように変更(2026-09-10)**
「一度選択すると候補一覧から消えるようにしてください。ただし、選択済み候補として見ることができるようにしてください」との依頼。既に「使用済みを隠す」チェック(既定OFF)は実装済みだったため、その既定値をONに変更するだけで対応(`eventPickerHideUsed`の初期値を`false`→`true`。モーダルを開くたびにこの既定に戻る)。チェックを外せば従来通り使用済みの候補も薄い表示・使用回数付きで確認できる。「くり返し使う」指定した候補は従来通り使用済みでも常に一覧に残る。モーダル内・マニュアルの説明文もこの既定動作に合わせて更新した。

## 2. 教科別の授業実績入力 — 対応済み(2026-09-07)

**きっかけ・目的**
現状、①〜⑥欄は「時限の便宜番号」を入力するだけで、どの教科(国語・数学など)かまでは記録していない(HANDOFF.md の「既知の未実装・今後の課題」にも記載)。教科ごとの授業実績を入れられるようにしたい。

**確認の結果・対応内容**
ユーザーに確認したところ、①〜⑥欄の数字(便宜番号)は時間割(曜日×時限→教科)を使った自動教科推定には使わない方針であることが判明。「予測」については、既存の「授業日課の内訳」で道徳・総合・特別活動・●等の記号別集計がすでにできていれば十分とのことだったため、複雑な時間割データ構造・予測ロジックは作らず、以下のシンプルな形で実装した。

- 「時数・集計」タブに「教科別実績の入力」パネルを新設。道徳・総合的な学習の時間・特別活動・音楽(既存の記号「道」「総」「学」「音」から自動集計済み)を除く8教科(国語・社会・数学・理科・美術・保健体育・技術家庭・外国語)について、実際に行った授業のコマ数(年度累計)を学年別に手入力できる(`state.subjectActuals`)
- 入力値は「必要時数との比較」表にも教科別の行として反映される(道徳・総合・特別活動・音楽の行は既存の自動集計のまま、教科ごとの行を追加)
- 既存の「教科(数字入力分の合計)」行(①〜⑥の数字入力の単純合計)はそのまま維持。教科別の手入力とは別集計である旨をヘルプ・注記に明記

**保留(今回は対応せず)**
- 時間割データ構造・残り授業日数からの自動予測ロジックは、ユーザーの意向により作らないことにした(必要になれば別途相談)。

## 3. Excel出力の書式を、実際に使っている行事予定ファイルの見た目に合わせて調整 — 対応済み(2026-09-07)

**きっかけ・目的**
現在アプリから出力されるExcelファイルは、セルの色が付いていない部分や、罫線が引かれていない部分がある。実際に学校で使っている(元になった)行事予定のExcelファイルの各シートを参考に、色付け・罫線などの書式を合わせたい。

**調査結果(原本ファイルを提供いただき分析)**
- 原本(4月職/4月生/年(職員)/年(生徒))は、見出し・データグリッドの全セルに隙間なく細罫線が入っており、結合セルも「隠れる側」のセルまで含めて罫線が設定されている。逆に、静的な色分け(土曜=青、日曜=赤など)は無く、色付けは「休/長休/欠」といった出席状況を条件付き書式で灰色表示する仕組みのみだった。
- アプリ側の罫線抜け・色抜けの原因は、見出し行や結合セルの「隠れる側」のマス、月間予定表の週番結合セルの陰の日、年間予定表で日数が31日に満たない月の余り部分などを、データを何も入れない(=罫線設定も無い)まま出力していたこと。

**対応内容**
- 月間予定表(教員用/家庭用)の見出し2行(row1/row2)で、結合セルの陰になる位置も含めて全マスに罫線付きの空セルを設定
- 週番のセル結合で「隠れる側」の日も罫線付きの空セルに変更(以前はnullで罫線が消えていた)
- 年間予定表(教員用/家庭用)の月見出し・年計/学期計/月計の結合セルの陰、および31日に満たない月の余りマスに、罫線付きの空セルを設定
- 土日祝の色分け(薄いグレー)は従来通り維持(原本の条件付き書式とは仕組みが異なるが、視認性のため残す方針)

**保留(今回は対応せず)**
- 原本にある「6列/3列ごとの太い罫線での区切り」(週・月のグループ境界)は、細罫線への統一のみで今回は見送り。必要なら別途対応可能。

## 4. 年間予定表Excel出力の印刷デフォルトを横向きに直す — 対応済み(2026-09-10)

**きっかけ・目的**
年間予定表のExcel出力(`exportAnnualXlsx`)は、現在ページ設定が縦向き(portrait)になっている。印刷時のデフォルトを横向き(landscape)に直したい。

**対応内容**
`exportAnnualXlsx`のpageOptsを`orientation:'landscape'`に変更。

## 5. 年間予定表Excel出力に見出し変更を反映/日・曜のずれを再調査 — 対応済み(2026-09-10)

**きっかけ・目的**
- 年間予定表(画面表示)の見出しは「日・曜・◯月行事」の3列に整理し、上にあった「◯月」だけの行は削除済み(2026-09-09対応)。Excel出力側(`buildAnnualExportGrid`)は未対応で、月見出し行+「日/曜/年間行事」の2行見出しのままになっている。画面表示と同じ見出し構成にしてほしいとの依頼。
- Excel出力の日・曜の表示位置について、2026-09-09に一度「上端そろえ」のスタイル(XSTYLE.CENTER_TOP等)を追加して対応したが、ユーザーから「まだずれている」との指摘あり。再調査が必要。Excelのセルはshrink-to-fit(折り返さず縮小表示)のため、HTML側(複数行に折り返した時に日・曜が縦中央に浮いて見える問題)とは原因が異なる可能性がある。実際に出力したExcelファイルを開いて見た目を確認しながら原因を特定すること。
- 【方針の追記・2026-09-10】行の高さを広げたことで、日・曜セルの中身は「上端そろえ」ではなく上下左右中央揃え(vertical="center" + horizontal="center")にしてほしいとのこと。上記の「上端そろえ」対応(XSTYLE.CENTER_TOP/HOLIDAY_CENTER_TOP/NONSCHOOL_CENTER_TOP)は方針転換により見直しが必要(日・曜セルは中央揃えに戻す。年間行事欄(LEFT系)の扱いは別途確認)。

**対応内容**
- `buildAnnualExportGrid`の見出しを1行(日・曜・◯月行事)にまとめ、月見出しの行を削除(HEAD構成・行番号の計算を1行分ずらして修正)。
- 日・曜セルは既存のXSTYLE.CENTER/HOLIDAY_CENTER/NONSCHOOL_CENTER(vertical="center" horizontal="center")に戻した(上端そろえのXSTYLE.*_TOP系は不使用に。項目7の折り返し対応と合わせて年間行事欄はXSTYLE.*_LEFT_WRAP系に変更)。
- `buildSheetXml`の行高さ計算が見出し2行決め打ちだったため、`pageOpts.headerRowCount`で見出し行数を指定できるようにし、年間予定表は1行を指定するよう修正(このバグが無いと、見出しを1行にまとめた影響で日付1行目の高さが誤って見出し帯の高さになっていた)。

**追記・原因判明(2026-09-10)**
実際に出力したExcelファイルをユーザーに送っていただき調査したところ、日・曜自体のデータは全月で正しく(データ・網掛けとも)出力されていることを確認。その後、ズームしたスクリーンショットで再確認したところ、実際にずれて見えていたのは「日」「曜」ではなく、見出し行の「◯月行事」ラベルだった。「◯月行事」見出しがXSTYLE.HEADER(中央揃え)のまま、その下の行事データ欄(XSTYLE.*_LEFT_WRAP、左揃え)と揃っていなかったため、見出しだけが右にずれて見えていた。見出し専用の左揃えスタイル(XSTYLE.HEADER_LEFT)を追加し、`buildAnnualExportGrid`の`◯月行事`ラベルに適用して解消。

## 6. 年間予定表Excel出力の年計・学期計・月計、桁数が違うとまだずれる — 対応済み(2026-09-10)

**きっかけ・目的**
`buildAnnualExportGrid`内の`sumLine`(日数の①②③表示)は、`padNum3`で半角スペースによる桁揃え(`String(n).padStart(3,' ')`)をしているが、出力フォントYu Minchoはプロポーショナル(等幅でない)フォントのため、スペース埋めでは実際の表示幅が揃わず、日数が1桁と2桁で混在すると①②③の位置がずれて見える。スペース埋め以外の方法(等幅フォントの利用、数値ごとにセルを分けて右揃えにする、等)を検討して直すこと。

**対応内容**
セルを分ける案は既存の結合セル構成(base+1〜base+3を1セルに結合)と相性が悪いため、フォントを等幅系の「MS Gothic」に変更する方針で対応(XSTYLE.SUMMARYが使うfontIdを差し替え。他のスタイルでは使っていないfontIdのため影響範囲はここのみ)。

## 7. 年間予定表Excel出力、行事欄の内容が多い時は改行で見やすくする — 対応済み(2026-09-10)

**きっかけ・目的**
年間予定表Excel出力の「年間行事」欄(LEFT/HOLIDAY_LEFT/LEFT_TOP等のスタイル)は現在`shrinkToFit="1"`(折り返さず、収まるようにフォントを縮小)になっており、内容が多い日は文字が小さくなりすぎて読みにくい。行の高さは既に広げてあるので、`wrapText="1"`(折り返し)に変更し、複数行で見やすく表示できるようにすること。行の高さとのバランス(折り返した結果、行がさらに高くなりすぎないか)も含めて検討する。

**対応内容**
既存のLEFT/HOLIDAY_LEFT/NONSCHOOL_LEFTは他の箇所(月間予定表のメモ一覧等)でも使われているため直接変更せず、年間行事欄専用の新しいスタイル(XSTYLE.LEFT_WRAP/HOLIDAY_LEFT_WRAP/NONSCHOOL_LEFT_WRAP。vertical="center" wrapText="1")を追加し、`buildAnnualExportGrid`の年間行事セルにのみ適用した。

## 8. 登校日チェック済みの土曜日は年間予定表(画面・Excelとも)も色を外す — 対応済み(2026-09-10、年間のみ)

**きっかけ・目的**
2026-09-09に、年間予定入力タブの土曜行について、いずれかの学年の「登校日」にチェックが入っている(実際に登校する)日は青い網掛けを付けないよう修正した(`isNonSchoolDay`とは別に、`dow===6 && !anyAttend`の判定を追加)。同じ考え方を年間予定表にも適用してほしいとの依頼。

**対応内容**
- `renderAnnualPreview`(画面表示)・`buildAnnualExportGrid`(Excel出力)の両方に、`isShadedDay`で祝日・土曜と判定された後、「祝日ではなく・土曜で・いずれかの学年が登校する」場合は`shaded`をfalseに戻す処理を追加。年間予定入力タブと同じ`anySchoolDay`を使用。
- 年間予定表の入力側(`buildAnnualExportGrid`/`renderAnnualPreview`)は共通で使う`isShadedDay`関数自体は変更せず(月間予定表など他の呼び出し箇所への影響を避けるため)、年間の2箇所で個別に判定を上書きする形にした。

**追記・月間も対応(2026-09-10)**
月間予定表(画面・Excelとも)にも同じ考え方を適用。`buildMonthlyExportGrid`(本表・翌月先取り欄の両方)と`renderMonthlyPreview`(画面表示。教員用・家庭用の翌月先取り欄含む)の計4箇所に、年間と同じ「祝日ではなく・土曜で・いずれかの学年が登校する」場合は`shaded`をfalseに戻す処理を追加。登校日チェックのON/OFFで、対象の土曜行の網掛けが即座に切り替わることを画面・Excel出力の両方でブラウザ上で確認済み。

**追記・判定条件を「いずれかの学年」から「全学年」に変更(2026-09-10)**
「一学年でもチェックが外れていたらセルの色はつけたまま(一部の学年だけ動いている。土曜に修学旅行がある場合など)」との指摘を受け、判定条件を変更。従来の`anySchoolDay`(いずれかの学年が登校日)の代わりに、新設した`allSchoolDay`(全学年が登校日)を使うように、年間・月間の網掛け判定6箇所(`buildAnnualExportGrid`/`renderAnnualPreview`/`buildMonthlyExportGrid`本表・翌月先取り/`renderMonthlyPreview`本表・翌月先取り)をすべて変更。掃除欄の「×」表示判定(`anySchoolDay`を使用。いずれかの学年が登校するのに掃除が未設定の時に表示)は意味が異なるため変更していない。一部の学年だけ登校日にチェックが入っている土曜は網掛けが残り、全学年にチェックが入っている土曜だけ網掛けが外れることをブラウザ上で確認済み。

## 9. 月間予定表Excel出力、見出しがはみ出す・2行にまたがるなど見づらい箇所を調整 — 対応済み(2026-09-10、給食欄のみ)

**きっかけ・目的**
月間予定表Excel出力(`buildMonthlyExportGrid`/`groupHeaderForExport`)で、「給食」欄の下にある「1年・2年・3年」の見出しがセルからはみ出して小さく表示されてしまう。他の項目の見出しも2行にまたがるなど見づらいものがあるため、列幅・フォントサイズ・折り返しなどを全体的に見直して調整すること。実際に出力したExcelファイルを開いて、見出し行を一つずつ確認しながら直す必要がある。

**対応内容**
`buildMonthlyExportGrid`内のcolWidths計算で、給食欄(grpLunch)の列幅を3→4.5に拡大(「1年」等の2文字見出しがはみ出さない幅を確保)。授業コマ(grade)の列幅も2.5→2.8に微調整。

**保留(今回は対応せず)**
- 「他の項目についても見づらいものがある」との指摘については、具体的にどの見出しかを実際のExcelファイルで確認できていないため、給食欄以外は未対応。次回、実際のExcel出力を見ながら該当箇所を特定して対応する。

## 10. 登校日にチェックが入っている日へ、授業・掃除・給食を一括入力する機能 — 対応済み(2026-09-10)

**きっかけ・目的**
年間予定入力タブで、現在「登校日」にチェックが入っている日(複数日)に対して、以下をまとめて一括入力できるようにしたい。
- 授業(①〜⑥に「1」「2」…「道」などを入力) — ただし、その日に登校日チェックが入っている学年の欄にのみ入力する(登校しない学年の欄は対象外)
- 掃除を「あり」にする
- 給食を「あり(○)」にする

**想定する使い勝手**
- 既存の「週番をまとめて入力」機能(モーダルで対象範囲を確認してから適用するUI)と同様に、実行前に「どの項目(授業/掃除/給食)を入れるか」を選択でき、対象になる日・学年を確認してから一括反映する、という確認ステップを設ける。
- 授業の内容(何を入れるか。例えば全コマ同じ内容にするのか、コマごとに指定するのか)は要検討・要相談。

**対応内容**
- 年間予定入力タブに「登校日へまとめて入力」ボタンを新設(`btnBulkAttendFill`)。押すと現在表示中の月を対象にしたモーダルが開く。
- モーダル内で①〜⑥の授業内容(datalist付き。空欄の項目は対象外)・掃除を入れるか・給食を入れるかをそれぞれチェックボックスで選べ、対象になる日数(学年別)がリアルタイムでプレビュー表示される(`renderBulkAttendFillPreview`)。「この内容で適用」を押すまで反映されない。
- 授業は、登校日チェックが入っている学年の欄のみ、かつ既に何か入力済みのマスは上書きしない(空欄のマスだけに入力)。掃除・給食は対象日・学年に一律で「あり」を設定する(上書きする)。
- 一括操作の前に`captureUndoPoint()`を呼んでおり、Ctrl+Zでまとめて元に戻せる。
- 授業の内容指定は、簡易的に「①〜⑥それぞれ1つの内容をその月全体に共通で適用」という方式にした(日によって内容を変えたい場合は個別入力が必要)。より柔軟な指定(曜日ごとに変える等)が必要になれば別途相談。

## 11. 画面右上のバージョン表示に更新時刻も入れる — 対応済み(2026-09-10)

**きっかけ・目的**
現在、画面右上のバージョンバッジは`APP_VERSION`(日付のみ、例: v2026-09-09)を表示している。同じ日に複数回更新した場合に見分けがつかないため、更新時刻まで表示するようにしてほしい。

**対応内容**
`APP_VERSION`定数を日付+時刻の形式(`YYYY-MM-DD HH:MM`)に変更。`updateFileVersionInfo()`側の表示ロジックはそのまま値を差し込むだけのため変更不要。更新の都度、この値を手動で書き換える運用は従来通り継続。

## 12. 特別支援学級(5組)の登校日チェックON/OFFで、授業欄の入力可否が切り替わらない — 対応済み(2026-09-10、バグ修正)

**きっかけ・目的**
特別支援学級を追加する機能(項目10の後に実装)で、登校日のチェックをON/OFFしても、5組の授業①〜⑥欄の入力可否(グレーアウト)が切り替わらない。1〜3年では正しく切り替わる。

**原因(特定済み)**
`handleCellChange`内の再描画トリガー判定が`/^g[123]SchoolDay$/`のまま(index.html:2396付近)。特別支援学級対応の際、`getFieldValue`/`setFieldValue`の正規表現は`[1234]`に拡張したが、この箇所を直し忘れていた。データ自体(`rec.schoolDay.g4`)は正しく保存されるが、`g4SchoolDay`はこの正規表現にマッチしないため`renderAgenda()`が呼ばれず、画面が再描画されない(タブ切り替えや月移動など、他の操作で再描画されるまで見た目が更新されない)。

**対応内容**
`/^g[123]SchoolDay$/` を `/^g[1234]SchoolDay$/` に修正(1行の修正)。5組の登校日チェックON/OFFで①〜⑥欄の入力可否が即座に切り替わることをブラウザ上で確認済み。

## 13. 給食喫食数の集計に5組を含める/時数・集計タブを給食と時数の2つに分割する — 対応済み(2026-09-10)

**きっかけ・目的**
- 特別支援学級(5組)を追加する機能では、年間予定表の集計・時数集計タブへの反映は対象外という方針にしたが、給食喫食数の集計だけは5組も含めてほしいとの依頼。
- 現在「時数・集計」タブの中に、給食関連(給食実施日数・年間給食喫食数・給食のない日一覧)と、時数関連(学校行事の回数・授業日課の内訳・曜日時限別実施回数・教科別実績入力・必要時数との比較・必要時数の編集)が1つのタブに混在している。これを別のタブ(シート)に分けてほしい。

**対応内容**
- `computeAnnualMealSummary()`・`computeStatsForRange()`の給食集計部分を`[1,2,3].forEach`から`activeGrades().forEach`に変更し、特別支援学級が有効な時は5組(g4)の給食実施日数・喫食数も集計対象に含めた(時数・授業日課関連のパネルは方針通り1〜3年のみのまま変更していない)。
- 「時数・集計」タブを「給食」タブと「時数・集計」タブの2つに分割。新設した「給食」タブ(`renderMealsTab()`)に給食実施日数・年間給食喫食数・給食のない日一覧の3パネルを移設し、特別支援学級が有効な時は5組の行・列を追加表示する(学級名は`state.meta.supportClassName`を使用)。「時数・集計」タブ(`renderStats()`)には学校行事の回数・授業日課の内訳・曜日時限別実施回数・教科別実績の入力・必要時数との比較のみを残した。
- 特別支援学級のON/OFFで給食タブの5組行・列が正しく出たり消えたりすることをブラウザ上で確認済み。

**追記・給食実施日数パネルをシンプル化(2026-09-10)**
「給食タブの給食実施日数のところは、4月〜3月、学期ごとの給食実施日数をまとめるだけでOKです」との依頼。従来は月選択(1か月分)+学期計+年間計の列に、給食○・給食×・授業日数・給食ない日の4種類の行を表示していたが、4月〜3月の12か月分の列+1学期・2学期・3学期・年間計の列に絞り、行も各学年の給食実施日数(給食○)のみに簡略化した。これに伴い、他のパネルからは参照されていなかった月選択(`mealsMonthSelect`)のUIと関連コード(`populateMonthSelectFor`呼び出し・change監視)も不要になったため削除した。

## 15. 月間Excel出力、「会議・出張・研修」「放課後」欄と給食見出しの幅・フォント調整 — 対応済み(2026-09-10)

**きっかけ・目的**
- 「会議・出張・研修」(`c.key==='meeting'`、現状の幅14)と「放課後」(`c.key==='club'`、現状の幅6)は、内容が長い場合はセルのサイズに合わせて縮小(shrinkToFit)される現状のままでよい。列幅を広げる必要はない。
- 給食欄の下にある「1年」「2年」「3年」「5組」の見出し(現状は`XSTYLE.HEADER`、太字10pt、幅4.5)は、まず①フォントサイズをもう少し小さくし、②列幅も詰められるところまで詰める、という方向で調整してほしい。もしそれでもうまく収まらない/見づらいようであれば、③「年」「組」を外して「1」「2」「3」「5」のように1文字表記にし、列幅をぎりぎりまで小さくする、というフォールバックでよい。

**対応内容**
- 給食見出し専用に8ptの太字フォント(fontId5)を使う新しいスタイル`XSTYLE.HEADER_SMALL`を追加し、日・曜の見出しと、給食欄(1年〜5組)・授業欄(①〜⑥)の列見出しに適用(他の見出し(会議・出張・研修等)には影響なし)。
- フォント縮小だけでは幅を十分に詰めきれなかったため、フォールバック案の通り、Excel出力のみ「1年」→「1」のように1文字表記にする`xlsxLabel`プロパティを`lunchG1`〜`lunchG4`に追加(画面上のプレビュー・年間予定入力タブの見出しは従来通り「1年」等のフル表記のまま。Excel出力にのみ影響)。
- 列幅の調整は項目16と合わせて対応(下記参照)。

**追記・見出しの折り返しを縮小表示に変更(2026-09-10)**
「会議・出張・研修」「放課後」の見出し自体は、内容が長い場合はセルのサイズに合わせて縮小(shrinkToFit)でよい、という当初の方針だったが、実装当初は見出しラベルに`wrapText`(折り返し)を使うスタイル(`XSTYLE.HEADER_T`)のままだった。ユーザーからの指摘を受け、見出し専用に`shrinkToFit`版のスタイル(`XSTYLE.HEADER_T_SHRINK`。放課後は罫線太め対応(項目17)との組み合わせで`HEADER_TR_SHRINK`)を追加し、この2つの見出しラベルにのみ適用した。

## 16. 月間Excel出力、日・曜・掃除・給食・授業・週番の列幅をすべて20ピクセルに統一 — 対応済み(2026-09-10)

**きっかけ・目的**
月間予定表Excel出力で、「日」「曜」「掃除」「給食」(1年〜5組それぞれ)「授業」(①〜⑥それぞれ)「週番」の列幅を、すべて20ピクセル相当に統一してほしい。

**対応内容**
Excelの列幅の「文字幅」単位への変換式`(px-5)/7`を使い、20ピクセル相当の文字幅(約2.1)を`NARROW_COL_WIDTH`として定義。日・曜・掃除・給食(1年〜5組)・授業(①〜⑥)・週番の列幅をすべてこの値に統一した(「会議・出張・研修」「放課後」は項目15の方針通り対象外のまま据え置き)。ブラウザ上で`buildMonthlyExportGrid`のcolWidths配列を確認し、対象列がすべて2.1、対象外列(events=60/meeting=14/club=6)が変わっていないことを確認済み。

**追記・微調整(2026-09-10)**
実際に出力したExcelを確認したユーザーから、縦書きの項目(掃除・週番の見出し)は中央揃えのままできれいに収まるが、横書きの項目(日・曜・給食・授業のデータや見出し)は20ピクセル相当だと`shrinkToFit`が働いて窮屈に見える、との指摘。左揃えにして隣の空セルへの「はみ出し表示」に頼る案も検討したが、隣接セルが埋まっている日はうまくはみ出せず表示が不安定になるため不採用とし、中央揃えのまま「縮小がかからない最小限の幅」まで個別に広げる方針にした。日(数字のみ、最大2桁)は2.3、曜・掃除・給食・授業(漢字1文字や「欠」等が入りうる欄)は2.8に調整。週番は氏名など複数文字が入ることが前提で縮小は避けられないため、20ピクセル相当(2.1)のまま据え置き。

**追記・ピクセル指定での再調整(2026-09-10)**
実際の見た目を確認したユーザーから、日・曜は26ピクセル、週番は27ピクセル相当にしてほしいとの指定があり、その通りに調整(掃除・給食・授業は2.8のまま変更なし)。週番は氏名など複数文字が入ることが前提のため、幅を広げても縮小自体は引き続き起こりうる。

## 17. 月間Excel出力、項目間の罫線を太め(濃いめ)にして境目を分かりやすくする — 対応済み(2026-09-10)

**きっかけ・目的**
月間予定表Excel出力で、項目のまとまり(グループ)の境目に太め(または濃いめ)の罫線を入れて、区切りが分かりやすいようにしてほしい。対象は「放課後と給食」「給食と授業」「授業と週番」の境目、および表全体の一番外側の枠線。すべて同じ太さにする。

**対応内容**
- `buildXlsxStyles`の`<borders>`に、黒色・中太(medium)の罫線を片側(または隣接する2辺)だけに持つborder定義を8種類追加(右太め/左太め/上太め/下太め、およびそれらの角の組み合わせ4種)。既存のhair罫線(borderId=1)はそのまま維持し、境目・外枠にあたるセルだけ新しいborderIdを使う`cellXfs`の派生スタイルを追加(既存スタイル30種それぞれについて、実際に境目・外枠に登場するもののみ、計30個追加)。
- `buildMonthlyExportGrid`で、放課後・給食の最終列(1〜3年、5組有効時は5組まで)・授業の最終列(同)・週番(教員用。家庭用は授業の最終列と共通)を境目の列として特定し、見出し行(row1/row2)・日ごとの行それぞれについて、該当列は右端を太く、表の左端(日列)は左端を太く、見出し行の一番上と最終日の行の一番下も太くする処理を追加(角にあたるセルは2辺分の太さを合成したスタイルを使用)。特別支援学級のON/OFF・教員用/家庭用のどちらでも、境目の列位置が正しく計算されることをブラウザ上で確認済み。
- 「日ごとのメモ」欄(改ページ後の別表)は対象外(今回の依頼は月間予定表本体のみのため)。

**追記・太すぎたため調整(2026-09-10)**
実際に出力したExcelを確認したユーザーから「太すぎる」との指摘があり、罫線の太さを`medium`から`thin`に変更(色は黒のまま)。既存のhair罫線(borderId=1)との区別はつきつつ、圧迫感を抑えた見た目にした。

**追記・さらに細く、学年間の境目も追加(2026-09-10)**
ユーザーから「さらに細く」との指摘を受け、色を黒(FF000000)からグレー(FF808080)に変更(太さはthinのまま。hairとの区別は保ちつつ圧迫感をさらに抑える)。また、1年授業|2年授業|3年授業|5組授業の学年間の境目にも同じ太さの罫線を追加(従来は最後の学年と週番の境目にしか入っていなかった)。`rightThickCols`の算出に、各学年(最後の学年を除く)の①〜⑥欄最終列を追加することで対応。

## 18. 月間・年間Excel出力、年号を和暦に統一 — 対応済み(2026-09-10)

**きっかけ・目的**
月間Excel出力の副題(例:「2026年6月」)が西暦のままだった。年間予定表・他のExcel見出しは令和表記なのに、ここだけ西暦になっていて統一感がない。

**対応内容**
`buildMonthlyExportGrid`の副題テキストを`${year}年${month}月`から`令和${year-2018}年${month}月`に変更。他の箇所(年間予定表のタイトル、出力日スタンプ等)はもともと令和表記だったため変更不要だった。

## 19. 月間・年間Excel出力、項目(見出し)セルの色が他の網掛け色と被らないよう調整 — 対応済み(2026-09-10)

**きっかけ・目的**
見出しセルの背景色(EFEFEF)が、休業日の網掛け色(既定D9D9D9)や集計行の色(ECECEC)と非常に近いグレー同士で、画面・Excelどちらも見分けにくいとの指摘。

**対応内容**
- Excel: 見出し背景を薄い青(FFDCE6F3)、年計・学期計・月計の集計行背景を薄い緑(FFE2EFDA)に変更(休業日網掛けは引き続きユーザー設定色のまま変更していない)。
- 画面表示: 月間・年間プレビューの見出し背景(`table.preview thead th`)も同じ薄い青に、年間予定表の年計・学期計・月計行(`tr.annual-summary`)も同じ薄い緑に、月計を強調する行(`tr.annual-summary-month`)はより濃い緑(#c6e0b4)に変更し、Excel出力と画面表示の配色を揃えた。

**追記・白黒印刷を踏まえて色を除去(2026-09-10)**
「基本的に白黒印刷になるので、色を使わないように調整してください」との依頼。どこまでが対象か確認したところ、今回追加した見出し・集計行の背景色(青・緑)のみが対象で、以前からある休業日・欠席の網掛け(グレー、設定で色変更可)と参考ページのタイトル帯(金色)は現状維持でよいとのこと。
- Excel: `XSTYLE.HEADER`系・`XSTYLE.SUMMARY`が使うfillId2/fillId4を、`patternType="solid"`(青・緑)から`patternType="none"`(塗りつぶしなし)に変更。見出しは太字+罫線、集計行は罫線(月計はより太い罫線)のみで区別する。
- 画面表示: `table.preview thead th`・`tr.annual-summary`・`tr.annual-summary-month`の背景色指定を削除し、代わりに`tr.annual-summary`に上罫線、`tr.annual-summary-month`により太い上罫線を追加して区別できるようにした。

## 20. 月間Excel出力、学校行事等の列幅をA4横向きにちょうど収まる8割程度に縮小 — 対応済み(2026-09-10)

**きっかけ・目的**
月間Excel出力がA4横向き1枚にちょうど収まらない。学校行事等の欄の幅を8割程度に狭くしてほしい。

**対応内容**
`buildMonthlyExportGrid`のcolWidthsで、学校行事等(events)の幅を60→48(60の8割)に変更。

## 21. 年間Excel出力、○月行事欄のフォントを縮小し、行事ごとに改行するように変更 — 対応済み(2026-09-10)

**きっかけ・目的**
- 年間Excelの「○月行事」欄のフォントが大きく、2行程度できれいに収まらない。
- 1日に複数の行事がある場合、全角スペース区切りだと折り返しの際に1つの行事の途中で改行されてしまい読みにくい。

**対応内容**
- 年間行事欄専用の8ptフォント(fontId6、通常のYu Mincho)を追加し、`XSTYLE.LEFT_WRAP`/`HOLIDAY_LEFT_WRAP`/`NONSCHOOL_LEFT_WRAP`(いずれも年間Excel専用スタイル)に適用。
- `annualEventText`(Excel出力専用。画面表示用の`annualEventHtml`は別関数で変更なし)で、複数行事の区切りを全角スペースから改行(`\n`)に変更。Excelの`wrapText`は明示的な改行を1行の区切りとして扱うため、行事ごとに独立した行として表示されるようになる(1つの行事自体が長い場合はその行事の中で折り返される)。

## 22. 年間Excel出力、その月に存在しない日(31日が無い月など)に斜線を引く — 対応済み(2026-09-10)

**きっかけ・目的**
年間Excelで、30日までしかない月の31日欄や、2月の30・31日欄など、その月に存在しない日の行は罫線だけの空欄になっている。原本のExcelのように斜線を引いて「存在しない日」であることを明示してほしい。

**対応内容**
`buildXlsxStyles`の`<borders>`に、四辺はhairのまま右下がりの斜線(diagonalDown)を追加したborder定義(id=10)を新設し、日・曜・行事欄それぞれに対応する派生スタイル(`XSTYLE.CENTER_DIAG`/`LEFT_DIAG`)を追加。`buildAnnualExportGrid`の`d>dim`(その月に存在しない日)の分岐で、この2つのスタイルを使うように変更(セル単位の斜線のため、3列(日・曜・行事)それぞれに引かれる形になる)。

## 23. 年間Excel出力、年計・学期計・月計を太字にしない/特別支援学級を④として追加(トグルで表示) — 対応済み(2026-09-10)

**きっかけ・目的**
- 年間Excelの年計・学期計・月計(①②③の集計行)が太字になっているが、太字にしないでほしい。
- 特別支援学級が有効な場合、④としてこの集計に追加してほしい(設定のON/OFFで表示・非表示が切り替わるように)。

**対応内容**
- `XSTYLE.SUMMARY`が使うフォントを、太字のMS Gothic(fontId4)から、太字なしのMS Gothic(fontId7、新規追加)に変更(等幅フォントによる桁揃えの効果はそのまま維持)。
- `buildAnnualExportGrid`内の学年別集計(`monthly`/`yearCum`/`termCum`)を、固定の`[1,2,3]`から`activeGrades()`ベースに変更。`sumLine`関数も可変長の配列を受け取り、①②③(特別支援学級が有効な時は④まで)を動的に生成するように書き換えた。特別支援学級のON/OFFで④の有無が切り替わることをブラウザ上で確認済み。
- 画面表示側(`renderAnnualPreview`のasRow等)は従来通り1〜3年のみのまま変更していない(依頼が「年間excel」に限定されていたため)。あわせて、ヘルプ文言(「特別支援学級の追加」)の「年計・学期計・月計...には反映されません」という記載が古くなったため、Excel出力には④として反映される旨に修正した。

## 24. 年間Excel出力、版・状態表示に【】を付ける — 対応済み(2026-09-10)

**きっかけ・目的**
年間Excelのタイトル2行目に添える版・状態表示(例:「正式版」)に、目立つように【】を付けてほしい。

**対応内容**
`buildAnnualExportGrid`のtitleLine2組み立て部分で、`　${annualVersionLabel}`だった箇所を`　【${annualVersionLabel}】`に変更。

## 25. 時数・集計タブ「必要時数との比較」、その他欄の削除/数字・●の自動集計化/標準時数合計との比較追加 — 対応済み(2026-09-10)

**きっかけ・目的**
- 「必要時数との比較(年間累計)」表の一番下にある「その他(行事予定にのらない分・手入力)」欄は不要なので削除してほしい。
- 「教科(数字入力分の合計)」の項目は、①〜⑥欄に実際に入力されている「1」〜「6」などの記号を集計する形にしてほしい(「●」も含めて)。
- 「設定」タブで設定されている標準時数(必要時数の合計)に対しての比較も追加してほしい。

**対応内容**
- 「その他(行事予定にのらない分・手入力)」の行(`state.extraHours`を手入力する欄)を削除(データ自体は保存ファイルに残るが、この表からは表示・編集できなくなる)。関連する`.extra-hours-input`のイベントリスナーも削除。
- 「教科(数字入力分の合計)」→「教科等(数字・●入力分の合計)」に改称し、集計対象を①〜⑥に入力された「1」〜「6」に加えて「●」(特別時間割)も含めるように変更(`subjectSum`関数)。「行」(行事によるカット)・「弁」(弁当)・「欠」(欠課)は授業時数ではないため引き続き対象外。
- 表の一番下に「合計(標準時数との比較)」の行を追加。実績は道徳・総合的な学習の時間・特別活動・音楽の自動集計と「教科等(数字・●入力分の合計)」を合算した値、必要時数は`REQUIRED_HOURS_SUBJECTS`(学習指導要領の全教科)の合計(＝学年の標準時数合計)とした(`compareRow`関数に`__grandTotal__`の分岐を追加)。手入力の「教科別実績の入力」は目安の別集計という位置づけのため、二重計上を避けるためこの合計には含めていない。
